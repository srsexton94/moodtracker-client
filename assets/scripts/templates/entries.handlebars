{{#each moods as |mood|}}
  <section data-id={{mood.id}}>
    <h4>{{mood.mood}}</h4>
    <p>Posted on: {{date-time mood.created_at}}</p>
    <button class="btn btn-outline-danger" data-id="{{mood.id}}">delete</button>
  </section>
{{/each}}
